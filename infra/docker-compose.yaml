services:
  flask_app:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: flask_app
    ports:
      - "5000:5000"
    environment:
      DB_USER: postgres
      DB_PASSWORD: noa10203040 
      DB_HOST: my-postgress-db.cc3m62swkfac.us-east-1.rds.amazonaws.com 
      DB_NAME: flask
      DB_PORT: 5432
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_REGION: us-east-1
      SECRET_KEY: dev_secret_key 

  test:
    container_name: test_app
    build:
      context: ../backend
      dockerfile: Dockerfile
    environment:
      DB_USER: postgres
      DB_PASSWORD: noa10203040
      DB_HOST: my-postgress-db.cc3m62swkfac.us-east-1.rds.amazonaws.com
      DB_NAME: flask
      DB_PORT: 5432
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_REGION: us-east-1
      SECRET_KEY: dev_secret_key
    command: ["pytest", "/app/app_test.py"] 
